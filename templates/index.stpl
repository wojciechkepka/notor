<table>
    <tr>
        <th>Id</th>
        <th>Title</th>
    </tr>
<% for note in notes.iter() { %>

    <tr>
        <td><%= note.note_id %></td>
        <td><%= note.title %></td>
    </tr>

<% } %>

</table>


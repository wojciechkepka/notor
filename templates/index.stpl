<table>
    <tr>
        <th>Id</th>
        <th>Title</th>
    </tr>
<% for (i, note) in notes.iter().enumerate() { %>

    <tr>
        <td><%= note.note_id %></td>
        <td><%= note.title %></td>
    </tr>

<% } %>

</table>

